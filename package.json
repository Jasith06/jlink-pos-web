{
  "name": "jlink-pos-web",
  "version": "1.0.0",
  "description": "JLINK POS System - Web Application",
  "main": "index.html",
  "scripts": {
    "dev": "python -m http.server 8000",
    "build": "echo 'Building static site' && mkdir -p dist && cp -r *.html styles/ js/ api/ assets/ dist/ || true",
    "start": "npx serve -s ."
  },
  "dependencies": {
    "@emailjs/browser": "^3.11.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  },
  "keywords": [
    "pos",
    "inventory",
    "qr-scanner",
    "firebase"
  ],
  "author": "JLINK Team",
  "license": "MIT"
}